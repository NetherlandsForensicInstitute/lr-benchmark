experiment:
  repeats: 1
  pairing:
    - name: cartesian
  pair_should_have_trace_and_reference_measurements: True
  scorer:
    - name: precalculated_asr
#    - name: logit
#      preprocessors:
#          - name: abs_diff
#          - name: rank_transformer
#    - name: lda
#      preprocessors:
#          - name: abs_diff
#          - name: rank_transformer
  calibrator:
    - name: elub_logit
  splitting_strategy:
    train_size: 0.8
    validate_size: .2
    n_splits: 1
  refnorm:
    refnorm_size: 20 # If empty, the trainset will be used as refnorm dataset, with the LOO method
