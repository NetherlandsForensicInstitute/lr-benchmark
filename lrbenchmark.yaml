experiment:
  repeats: 1
  scorer:
    - logit
  calibrator:
    - logit
  splitting_strategy:
    train_size: .8
    validate_size: .2
    n_splits: 2
  preprocessor:
    pipeline:
      rank_transformer: rank_transformer