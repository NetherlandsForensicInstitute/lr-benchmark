experiment:
  repeats: 1
  pairing:
    - name: balanced
  scorer:
#    - name: precalculated_asr
    - name: logit
      preprocessors:
          - name: abs_diff
#          - name: rank_transformer
#    - name: lda
#      preprocessors:
#          - name: abs_diff
#          - name: rank_transformer
  calibrator:
    - name: elub_logit
  splitting_strategy:
#    leave_two_out: True
    train_size: 0.6
#  refnorm:
#    refnorm_size: # If empty, the trainset will be used as refnorm dataset, with the leave one out method
